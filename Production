#Чтобы развернуть готовый проэкт на удаленном хостинге:

Создаем на удаленном сервере Git-репозиторий и рядом с ним папку, где располагается веб-сайт.

На локальном компьютере делаем:
git add .
git commit -m "..."
git push
Проект копируется в удаленный репозиторий.

На удаленном:
git pull
и проект из Git репозитория переносится в текущую папку с сайтом.

Далее в .env.local на сервере прописываем переменные окружения для него.
